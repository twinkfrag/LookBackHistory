<UserControl x:Class="LookBackHistory.Views.SearchTabItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LookBackHistory"
			 xmlns:System="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
			 MouseDoubleClick="UserControl_MouseDoubleClick">
	<ListBox Name="dataList" ItemsSource="{Binding Path=Histories, Mode=OneWay}" >
		<ItemsControl.Template>
			<ControlTemplate TargetType="ItemsControl" >
				<DockPanel>
					<DataGrid Name="dataColumnsGrid" CanUserResizeColumns="False" DockPanel.Dock="Top" 
						ItemsSource="{Binding Path=Histories}" ColumnWidth="*"
						AutoGeneratingColumn="DataGrid_AutoGeneratingColumn" AutoGeneratedColumns="DataGrid_AutoGeneratedColumns">
						<!--<DataGrid.Columns>
							<DataGridTextColumn Header="Title" Width="*" />
							<DataGridTextColumn Header="Url" Width="*" />
							<DataGridTextColumn Header="Access" Width="*" />
						</DataGrid.Columns>-->
						<DataGrid.Template>
							<ControlTemplate>
								<DataGridColumnHeadersPresenter />
							</ControlTemplate>
						</DataGrid.Template>
					</DataGrid>
					<ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
						<ItemsPresenter Margin="0, 5" />
					</ScrollViewer>
				</DockPanel>
			</ControlTemplate>
		</ItemsControl.Template>

		<ItemsControl.ItemsPanel>
			<ItemsPanelTemplate>
				<WrapPanel />
			</ItemsPanelTemplate>
		</ItemsControl.ItemsPanel>

		<ItemsControl.ItemTemplate>
			<DataTemplate>
				<TextBlock Margin="5" MaxWidth="300">
					<Run Text="Title: " />
					<Run Text="{Binding Title}" />
					<LineBreak />
					<Run Text="URL: " />
					<Run Text="{Binding Url}" />
					<LineBreak />
					<Run Text="Access: " />
					<Run Text="{Binding LastAccess}" />
				</TextBlock>
			</DataTemplate>
		</ItemsControl.ItemTemplate>
	</ListBox>
</UserControl>
